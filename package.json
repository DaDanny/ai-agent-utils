{
  "name": "ai-agent-utils",
  "version": "1.0.3",
  "description": "CLI utilities for Danny's AI coding agent workflow",
  "bin": {
    "generate-instructions": "./dist/generate-instructions.js"
  },
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DaDanny/ai-agent-utils.git",
    "directory": "."
  },
  "scripts": {
    "build": "tsup",
    "prepare": "npm run build",
    "test": "vitest run"
  },
  "files": [
    "dist",
    "bin",
    "lib",
    "!**/*.ts"
  ],
  "sideEffects": false,
  "author": "DaDanny",
  "license": "MIT",
  "dependencies": {
    "date-fns": "^3.0.0"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.0.0",
    "vitest": "^3.2.2"
  }
}
